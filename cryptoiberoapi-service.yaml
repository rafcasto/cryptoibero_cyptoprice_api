apiVersion: v1
kind: Service
metadata:
  name: cryptoibero-svc
  labels:
    app: cryptoibero-api
spec:
  type: LoadBalancer 
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
  selector:
    app: cryptoibero-api